{
  "name": "workbuddy-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "WorkBuddy desktop quota → local OpenAI-compatible API + web UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "undici": "^6.28.0"
  }
}
